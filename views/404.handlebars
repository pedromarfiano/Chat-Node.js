
<head>
    <title>404</title>
    <link rel="stylesheet" href="/css/vars.css">
    <style>
        body{
            background-color: var(--white3);
            color: var(--text1);

            font-family: 'Open Sans', sans-serif;
            font-size: large;
            text-align: center;
        }
        a{
            text-decoration: none;
            color: var(--active);
        }
        h1{
            width: 500px;
            max-width: 100%;

            padding: 30px;
            margin: auto;

            border-top: solid 6px var(--text2);
            border-bottom: solid 6px var(--text2);
        }
        button{
            background-color: var(--active);

            max-width: 40%;
            width: 150px;
            height: 45px;

            border: solid 2px var(--text2);
            border-radius: 50px;

            color: var(--msg1);
            font-size: 20px;

            box-shadow: #111111cb 10px 10px 5px;
        }
        button p{
            margin: 0%;
        }
    </style>
</head>
<body>
    <main>
        <h1>Ops! Erro 404</h1>
        <h2>Página não encontrada!</h2>
        <a href="/"><p>Ir para página principal</p></a>
        <button class="btn_theme">
            <p>Alterar Tema</p>
        </button>
    </main>
</body>
<script src="/js/ThemeMode.js"></script>